export const types = `#graphql

     input CreateTaskInput {
        name: String!
        taskId: String!
        description: String!
        points: Int!
        media: String
        type: String!
        
    }

`
