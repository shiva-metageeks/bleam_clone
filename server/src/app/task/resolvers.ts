

const queries = {
    getTask: async () => {
        
        return 2;
    }
}

const mutations = {
    createTask: async () => {
        // const task = await TaskService.createTask(payload);
        return 2;
    }
}

export const resolvers = {
    queries,
    mutations
}
